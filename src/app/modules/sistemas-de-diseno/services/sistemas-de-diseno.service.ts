import { Injectable } from '@angular/core';

/**
 * Servicio para gestionar la información de los sistemas de diseño.
 *
 * Actualmente no contiene operaciones porque el módulo solo presenta la
 * estructura inicial para incorporar esta información posteriormente.
 *
 * @remarks
 * Este servicio está registrado a nivel raíz (`providedIn: 'root'`) y puede
 * ser inyectado en los componentes del módulo cuando se agreguen fuentes
 * de datos y métodos de consulta.
 */
@Injectable({
  providedIn: 'root',
})
export class SistemasDeDisenoService {
  
}
